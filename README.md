# speech-based-search-engine
This Python script is a voice-activated assistant that listens to user input, converts it to text using speech recognition, and performs a Google search based on the query. It uses `pyttsx3` for voice responses and `webbrowser` to open search results. It includes basic error handling for recognition issues.
